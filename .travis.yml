language: node_js

node_js:
  - '14'

cache:
  directories:
    - ~/.npm

notifications:
  email: false

install:
  - travis_retry npm install

script:
  - npm test

jobs:
  include:
    - stage: test
    - stage: release
      script: npm run release

stages:
  - test
  - name: release
    if: branch = master AND type = push AND fork = false

branches:
  except:
    - /^v\d+\.\d+\.\d+$/
